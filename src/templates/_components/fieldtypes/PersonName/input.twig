{#
/**
 * NSM Fields plugin for Craft CMS 3.x
 *
 * Person Name Field Input
 *
 * @author    Leevi Graham
 * @copyright Copyright (c) 2017 Leevi Graham
 * @link      http://newism.com.au
 * @package   NsmFields
 * @since     1.0.0
 */
#}

{% import "_includes/forms" as forms %}

<style>
    .personName {
        display: flex
    }

    .personName label {
        font-size: 11px;
        display: block;
    }

    .personName input {
        width: 100%;
    }

    .personName > div {
        flex: 1 0 auto;
    }

    .personName > div:first-child,
    .personName > div:last-child {
        flex: 0 0 auto;
    }

    .personName > div + div {
        margin-left: 5px;
    }
</style>

<div class="personName">
    <div>
        <label for="">Prefix</label>
        {{ forms.text({
            id: name ~ '-honorificPrefix',
            name: name ~ '[honorificPrefix]',
            value: value ? value.honorificPrefix,
            class: 'nicetext',
            style:"width: 6ch"
        }) }}
    </div>
    <div>
        <label for="">Given Names</label>
        {{ forms.text({
            id: name ~ '-givenNames',
            name: name ~ '[givenNames]',
            value: value ? value.givenNames,
            class: 'nicetext'
        }) }}
    </div>
    <div>
        <label for="">Additional Names</label>
        {{ forms.text({
            id: name ~ '-additionalNames',
            name: name ~ '[additionalNames]',
            value: value ? value.additionalNames,
            class: 'nicetext'
        }) }}
    </div>
    <div>
        <label for="">Family Names</label>
        {{ forms.text({
            id: name ~ '-familyNames',
            name: name ~ '[familyNames]',
            value: value ? value.familyNames,
            class: 'nicetext'
        }) }}
    </div>
    <div>
        <label for="">Suffix</label>
        {{ forms.text({
            id: name ~ '-honorificSuffix',
            name: name ~ '[honorificSuffix]',
            value: value ? value.honorificSuffix,
            class: 'nicetext',
            style:"width: 12ch"
        }) }}
    </div>
</div>
